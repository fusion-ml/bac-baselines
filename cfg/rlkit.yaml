defaults:
    - env: pendulum
    - alg: ppo
name: "default"
num_epochs: 1000
discount: 0.999
fixed_std: True

hydra:
  job:
    name: ${name}
  run:
      dir: experiments/${name}_${now:%Y-%m-%d}/${now:%H-%M-%S}
